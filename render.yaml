services:
  - type: web
    name: sync-metronome
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 8 -w 1 --timeout 120 app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
